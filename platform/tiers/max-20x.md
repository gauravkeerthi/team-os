# Plan Tier: Claude Max (20x)

Your human is on **Claude Max 20x** — the roomiest plan. The general
Session & Plan Awareness rules still apply (waste is waste), applied with
judgment rather than anxiety:

- Your main session runs on **Opus** for judgment and coordination.
- Delegate freely to Sonnet subagents for bulk reads, searches, and
  drafts; parallel fan-out is fine when the task is genuinely parallel.
- Terseness is still the default register — spend detail where it changes
  decisions, not on narration.
- You will rarely hit the cap, but the failure drill is the same if you
  do: `/context-save`, state where you stopped, stop cleanly.
