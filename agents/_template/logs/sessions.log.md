# Session Ledger — __AGENT_NAME__

> One line per session start and end. Written by `ops/launch.sh` and the
> `/close` skill. `tos status` summarizes the last 7 days.
> Format:
>   `- <ISO> start (tier=<plan> model=<model>)`
>   `- <ISO> end (~<minutes>m)` — append ` limit-hit` if the plan cap was hit.

<!-- ledger lines below -->
